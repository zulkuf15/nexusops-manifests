apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: nexusops-frontend-monitor
  namespace: nexusops-dev
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: nexusops-frontend
  endpoints:
  - port: metrics
    interval: 30s